{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "Literal[value=/stars?/i]",
        "message": "Never use the word 'stars' — always use 'gems'. See AGENTS.md rule #1."
      },
      {
        "selector": "Identifier[name=/[Ss]tar/]",
        "message": "Never use 'star' in identifiers — always use 'gem'. See AGENTS.md rule #1."
      }
    ]
  }
}
